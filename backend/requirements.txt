# Web framework + server
fastapi>=0.110
uvicorn[standard]>=0.27

# Database
sqlalchemy>=2.0
psycopg2-binary>=2.9       # for PostgreSQL on Render (Day 3 deploy)
alembic>=1.13              # schema migrations — single source of truth is models.py

# Validation & settings
pydantic[email]>=2.5       # [email] pulls in email-validator, required by EmailStr
pydantic-settings>=2.1
python-dotenv>=1.0

# Auth
bcrypt>=4.1
itsdangerous>=2.1          # signed session cookies (used by SessionMiddleware)

# Multipart / form uploads (audio files on Day 2)
python-multipart>=0.0.7

# AI providers — speaking evaluation pipeline
openai>=1.40                         # Whisper transcription + GPT-4o grading
azure-cognitiveservices-speech>=1.40 # Pronunciation assessment

# Email (Day 3)
aiosmtplib>=3.0
tzdata